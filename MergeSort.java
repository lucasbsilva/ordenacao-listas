import java.util.ArrayList;
import java.util.List;

class MergeSort<T extends Comparable<T>> implements IEstrategiaOrdenacao<T> {
    
    @Override
    public List<T> ordenar(List<T> lista) {
        // Lógica de ordenação com Merge Sort
        return lista;
    }
}