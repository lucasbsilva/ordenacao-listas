import java.util.ArrayList;
import java.util.List;

class BubbleSort<T extends Comparable<T>> implements IEstrategiaOrdenacao<T> {
   
   @Override
    public List<T> ordenar(List<T> lista) {
        // Lógica de ordenação com Bubble Sort
        return lista;
    }
}